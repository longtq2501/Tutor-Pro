# MySQL Database Configuration
spring:
  datasource:
    # URL cơ sở dữ liệu
    url: jdbc:mysql://yamanote.proxy.rlwy.net:28075/railway
    # Tên người dùng MySQL
    username: root
    # Mật khẩu MySQL
    password: ZxcrDghnhFCiDFGmyxcNpNeDAWwkjhMC
    driver-class-name: com.mysql.cj.jdbc.Driver

  # JPA/Hibernate Configuration
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
        format_sql: true

  # File upload configuration
  servlet:
    multipart:
      enabled: true
      max-file-size: 50MB
      max-request-size: 50MB

  mail:
    host: smtp.gmail.com
    port: 587
    username: tonquynhlong05@gmail.com
    password: ioby qhsi cuoi lwmu
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
            required: true

# Application specific configuration
app:
  mail:
    from: tonquynhlong@gmail.com
    from-name: Thầy Quỳnh Long

  # Data Initialization Configuration
  init-data:
    enabled: true                    # Bật/tắt tự động tạo data
    default-password: password123    # Password mặc định cho demo accounts

# File storage location
file:
  upload-dir: uploads

# JWT Configuration
jwt:
  secret: 404E635266556A586E3272357538782F413F4428472B4B6250645367566B5970337336763979244226452948404D635166546A576E5A7234753778214125442A
  expiration: 86400000              # 24 hours in milliseconds
  refresh-expiration: 604800000     # 7 days in milliseconds

# Server Configuration
server:
  # Cổng của ứng dụng Spring Boot
  port: 8080
  error:
    include-message: always
    include-binding-errors: always

# CORS Configuration (cho phép tất cả origins vì chỉ bạn dùng)
cors:
  allowed-origins: 'https://tutor-management-e7zh.vercel.app'

# Logging
logging:
  level:
    com:
      tutor_management: DEBUG
    org:
      springframework:
        security: DEBUG

# Cloudinary Configuration
cloudinary:
  cloud-name: ${CLOUDINARY_CLOUD_NAME:dofwiy3t}
  api-key: ${CLOUDINARY_API_KEY:816839713568739}
  api-secret: ${CLOUDINARY_API_SECRET:NZiSr1Kv2b3KME98NB_rWE66ybw}